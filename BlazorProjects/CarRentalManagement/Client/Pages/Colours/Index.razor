@page "/colours/"
@attribute [Authorize]
    <HeadingComponent Text="Boje" />
    <br />
    <a href="/colours/create" class="btn btn-secondary">
        <span class="oi oi-plus"></span>
        Unos nove boje
    </a>
    <br />
    <br />
    @if (Colours == null)
    {
        <div class="alert alert-info">Učitavanje boja...</div>
    }
    else
    {
        <table class="table ">
            <thead>
                <tr>
                    <th>Naziv</th>
                    <th>Akcije</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var colour in Colours)
                {
                    <tr>
                        <td>@colour.Name</td>

                        <td>
                            <a href="/colours/view/@colour.Id" class="btn btn-primary">
                                <span class="oi oi-book"></span>
                            </a>
                            <a href="/colours/edit/@colour.Id" class="btn btn-warning">
                                <span class="oi oi-pencil"></span>
                            </a>
                            <button class="btn btn-danger" @onclick="@(()=>Delete(colour.Id))">
                                <span class="oi oi-delete"></span>
                            </button>

                        </td>
                    </tr>

                }
            </tbody>
        </table>
    }


    @code {
        
    }
